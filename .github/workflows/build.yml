name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  py:
    name: py (reusable)
    uses: CoderDeltaLAN/ci-matrix-starter/.github/workflows/py-ci.yml@feat/ci-reusables-solid
    with:
      python_versions: '["3.11","3.12"]'
      os: '["ubuntu-latest"]'
      run_tests: true
    secrets: inherit

  ts:
    if: ${{ hashFiles('**/package.json') != '' }}
    name: ts (reusable)
    uses: CoderDeltaLAN/ci-matrix-starter/.github/workflows/ts-ci.yml@feat/ci-reusables-solid
    with:
      node_versions: '["22.x"]'
      os: '["ubuntu-latest"]'
      package_manager: "pnpm"
      test_command: "npm test --if-present"
    secrets: inherit
